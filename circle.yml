machine:
  node:
    version: 0.10.26
test:
  pre:
    - API_ENV=staging node bin/status
    - API_ENV=production node bin/status
  override:
    - case $CIRCLE_NODE_INDEX in 0) export API_ENV=staging; npm test ;; 1) export API_ENV=production; npm test ;; esac:
        parallel: true
